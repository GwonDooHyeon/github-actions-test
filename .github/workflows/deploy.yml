name: Github Actions CI/CD

on:
  push:
    branches:
      - main

jobs:
  my-build:
    runs-on: ubuntu-latest

    steps:
      - name: Hello world 출력
        run: echo "Hello world"

      - name: 여러 명령어 문장 작성
        run: |
          echo "Good"
          echo "Morning"
